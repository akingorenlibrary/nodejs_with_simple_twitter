<div class="container " style="margin-top:20px">
    <div class="card" style="background-color: white;">
        <div class="card-header">Profil / Ayarlar / Image Upload</div>
        <div class="card-body">
            <div class="row" style="text-align:center">
                <div class="col-md-12">
                    {{#if user_image}}
                    <img src="/user-images/{{user}}/{{user_image}}" width=100 height=100 class="rounded float-start" alt="...">
                    {{else}}
                    <img src="/images/user.png" width=100 height=100  class="rounded float-start" alt="...">
                    {{/if}}
                </div>
            </div>
            <div class="row" style="text-align:center;margin-top:40px">
                <div class="col-md-12">
                    <form action="/{{user}}/image-upload" method="POST" enctype="multipart/form-data">

                        <div class="custom-file">
                            <input type="hidden" name="click" value="{{clickValue}}">
                            <input type="file" id="image" class="custom-file-input" name="image" id="customFile">
                            <label class="custom-file-label" for="customFile">Fotoğraf seç</label>
                        </div>
                        <br><br>
                        <button type="submit" id="buttonBlock" class="btn btn-primary">Kaydet</button>
                        <a type="button" id="buttonBlock" href="/{{user}}/image-upload/image-remove" class="btn btn-secondary btn-sm">Profil resmini kaldır</a>
                    </form>
                </div>
            </div><br><br>
            <div class="row">
                <div class="col-md-12">
                     <p>Resim JPG veya PNG formatında olmalıdır.<br>Resim 10 MB boyutunu aşmamalıdır.</p>
                </div>
            </div>
        </div>
    </div>
</div>
</div>