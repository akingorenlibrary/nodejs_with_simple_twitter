<div class="container" style="margin-top:25px">
    <div class="card">
        <div id="chat-wrap">
            <h5 style="padding:5px;text-align:center" id="receiver">{{receiver}}</h5>
            <input type="hidden" value="{{user}}" id="user">
            <hr>
            <div id="chat-window" style="margin-left:5px">
                {{#each result}} 
                    {{#if kalin}}
                   <b> {{sender}} : {{message}}</b><br>
                    {{else}}
                    {{sender}} : {{message}}<br>
                    {{/if}}
                {{/each}}
                <div id="output"></div>
            </div>
            <hr>
            <div class="input-group mb-3" style="padding:5px">
                <input type="text" class="form-control" id="message" placeholder="Mesaj yaz">
                <button class="btn btn-outline-primary" id="gonderbtn" type="button" id="button-addon2">Gönder</button>
            </div>
        </div>
        
    </div>
</div>

